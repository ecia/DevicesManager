@using DevicesManager.Models;
@model DevicesManager.Models.AddViewModel
@{
    ViewBag.Title = "Zarejestruj";
}

<h2>@ViewBag.Title.</h2>

@using (Html.BeginForm("Register", "Account", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{
    @Html.AntiForgeryToken()
    <h4>Utwórz nowe konto.</h4>
    <hr />
    @Html.ValidationSummary("", new { @class = "text-danger" })

    @*@Html.Partial("~/Views/Account/_RegisterForm.cshtml", Model.RegisterViewModel );*@

    @*<div class="form-group">
        @Html.LabelFor(RoleName.Admin, new { @class = "col-md-2 control-label" })
        <div class="col-md-10">
            @Html.DropDownListFor(m => m.RoleName, new { @class = "form-control" })
        </div>
    </div>*@

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" class="btn btn-default" value="Zarejestruj" />
        </div>
    </div>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}